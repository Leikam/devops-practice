apiVersion: v1
kind: Pod
metadata:
  name: conv-app
  labels:
    module: frontend
spec:
  containers:
    - name: conv-app
      image: antonlarichev/conv-app:1.2
      ports:
        - containerPort: 80
      resources:
        limits:
          cpu: 500m
          memory: 512Mi
